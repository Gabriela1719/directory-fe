interface RoutePaths {
    login: string;
    register: string; 

    adresar: string;
    favorite: string;
    addNewContact: string;
}

export const routePaths: RoutePaths = {
    login: 'login',
    register: '/register',

    adresar: '/adresar',
    favorite: '/favorite',
    addNewContact: '/add',
}